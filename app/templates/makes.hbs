<div class="container-fluid">
	<div class="row">
		<div class="col-sm-4 col-md-3">
			<ul class="nav nav-pills nav-stacked">
			{{#each model as |make|}}
				<li>{{link-to make.name 'makes.models' make.id}}</li>
			{{/each}}
			</ul>
			<br>
			{{add-item store=store}}
		</div>
		<div class="col-sm-8 col-md-9">
			{{outlet}}
		</div>
	</div>
</div>